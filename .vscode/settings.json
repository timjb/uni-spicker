// Place your settings in this file to overwrite default and user settings.
{
  // To make snippets work (otherwise the autosuggestion will have
  // words from the file at the top instead of asking to expand a snippet)
  "editor.wordBasedSuggestions": false
}